export function DetourIcon({ size = 28, className = "" }: { size?: number; className?: string }) {
  return <img src="/icons/detour-192.png" width={size} height={size} className={`detour-icon ${className}`} alt="" />;
}
